name: heartbeat
on:
  schedule:
    - cron: "*/11 * * * *"
  workflow_dispatch:
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - run: curl -fsS https://ai-support-agent-1edp.onrender.com/health || true
